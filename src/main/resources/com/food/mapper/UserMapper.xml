<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.food.mapper.UserMapper">

    <resultMap id="UserMap" type="com.food.entity.User">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="gender" column="gender"/>
        <result property="phone" column="phone"/>
        <result property="email" column="email"/>
        <result property="address" column="address"/>
        <result property="avatarFileName" column="avatar_file_name"/>
        <result property="role" column="role"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="styles" column="styles" typeHandler="com.food.handler.ListToStringTypeHandler"/>
    </resultMap>

    <insert id="insertUser" parameterType="com.food.entity.User">
        INSERT INTO users (
            username, password, gender, styles,
            phone, email, address, avatar_file_name, role, created_at
        ) VALUES (
                     #{username}, #{password}, #{gender},
                     #{styles,typeHandler=com.food.handler.ListToStringTypeHandler},
                     #{phone}, #{email}, #{address}, #{avatarFileName}, #{role},
                     NOW()
                 )
    </insert>

    <select id="countByEmail" parameterType="String" resultType="int">
        SELECT COUNT(*) FROM users WHERE email = #{email}
    </select>

    <select id="selectByUsername" parameterType="String" resultMap="UserMap">
        SELECT * FROM users WHERE username = #{username}
    </select>

    <select id="countByUsername" parameterType="String" resultType="int">
        SELECT COUNT(*) FROM users WHERE username = #{username}
    </select>

    <select id="countAllUsers" resultType="int">
        SELECT COUNT(*) FROM users
    </select>

    <select id="selectAllUsers" resultMap="UserMap">
        SELECT * FROM users ORDER BY created_at DESC
    </select>

    <update id="updateUserStatus">
        UPDATE users SET status = #{status} WHERE id = #{id}
    </update>

    <update id="updateAvatarByUsername">
        UPDATE users
        SET avatar_file_name = #{avatarFileName}
        WHERE username = #{username}
    </update>

    <update id="updateUser" parameterType="com.food.entity.User">
        UPDATE users
        SET
            username = #{username},
            phone = #{phone},
            email = #{email},
            gender = #{gender},
            address = #{address},
            avatar_file_name = #{avatarFileName},
            styles = #{styles,typeHandler=com.food.handler.ListToStringTypeHandler}
        WHERE id = #{id}
    </update>

    <select id="selectByEmail" resultType="com.food.entity.User">
        SELECT * FROM users WHERE email = #{email}
    </select>

    <update id="updatePasswordByEmail">
        UPDATE users SET password = #{password} WHERE email = #{email}
    </update>

    <delete id="deleteUserById">
        DELETE FROM users WHERE id = #{id}
    </delete>
</mapper>